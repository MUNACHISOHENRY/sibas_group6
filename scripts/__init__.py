# Makes `scripts/` importable as a package so the admin dashboard can
# call into scripts/script2_register.py directly. Script 2 stays a
# named deliverable that can also be run from the command line.
